const IconText = ({ children }) => {
   return (
      <div className="text-xs text-slate-500 dark:text-slate-300 flex items-center space-x-1">
         {children}
      </div>
   );
};

export default IconText;
